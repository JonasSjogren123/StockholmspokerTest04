package com.example.stockholmspokertest04

import android.widget.ImageView
import android.widget.LinearLayout

class PlayingCardGallery {
    /*
    val playingCardSlot1 = PlayingCardSlot(R.drawable.gallery_playing_card_slot_empty)
    val playingCardSlot2 = PlayingCardSlot(R.drawable.gallery_playing_card_slot_empty)
    val playingCardSlot3 = PlayingCardSlot(R.drawable.gallery_playing_card_slot_empty)
    val playingCardSlot4 = PlayingCardSlot(R.drawable.gallery_playing_card_slot_empty)
    val playingCardSlot5 = PlayingCardSlot(R.drawable.gallery_playing_card_slot_empty)

    val listOfPlayingCardSlots = ListOf<PlayingCardSlot>(playingCardSlot1,playingCardSlot2,playingCardSlot3,playingCardSlot4,playingCardSlot5)

    placePlayingCardSlotsInGallery()

    fun placePlayingCardSlotsInGallery() {

        if (buttonIsAlreadyPressed == true) {
            layout?.removeAllViewsInLayout()
        }

        for (playingCard in playingCardDeck.listOfPlayingCardSlots) {
            val imageView = ImageView(this)
            imageView.layoutParams = LinearLayout.LayoutParams(200, 200)
            flipCardSide()
            if (playingCardFaceUp == true) {
                imageView.setImageResource(playingCard.imageFace)
            } else {
                imageView.setImageResource(playingCard.cardSideBack)
            }

            placeViewRandomly(imageView)
            rotateViewRandomly(imageView)
            layout?.addView(imageView)
            imageView.tag = playingCard
        }
        buttonIsAlreadyPressed = true
        /*val button = findViewById<Button>(R.id.button1)
            layout?.addView(button)*/
    }*/

}